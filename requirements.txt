# 此文件由 pyproject.toml 维护，推荐使用 uv 管理依赖：
#   uv sync
# 若仍需 pip 兼容安装，可执行：
#   uv export --no-hashes -o requirements.txt && pip install -r requirements.txt

# RoleSwap 数字人长视频生成封装库依赖
requests>=2.31.0
ffmpeg-python>=0.2.0
opencv-python>=4.8.0
numpy>=1.24.0
python-dotenv>=1.0.0

# Web 测试页面
flask>=3.0.0
gunicorn>=22.0.0

# 开发 / lint
ruff>=0.6.0
