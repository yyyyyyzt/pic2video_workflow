# 技术路线讨论与决策（设计文档）

> 记录选型的完整依据与取舍。所有结论基于 2026-07 的实际调研（官方仓库源码、
> ComfyUI 原生节点源码、阿里云百炼 API 文档、论文）。

**当前决策一句话**：口播场景以**自托管 Wan2.2-Animate** 为主引擎；SCAIL-2 降为
备选（复杂 3D 动作/非人角色）；百炼 API 作为不需要 GPU 的验证与实验通路，其中
`wan2.7-videoedit` 通过参考图伪锚定可跑长视频（实验性）。

---

## 1. 模型家族关系：SCAIL-2 与 Wan 不是版本竞争

SCAIL-2 是智谱（Z.AI）**基于 Wan 2.1 14B DiT 底座**训练的专用模型，所以整套
组件都是 Wan2.1 的（`wan2.1_14B_SCAIL_2_*.safetensors`、`Wan2.1_VAE`、`umt5-xxl`）。
更关键的一层：SCAIL-2 的训练数据 MotionPair-60K 是用 SCAIL-Preview +
**Wan-Animate** + MoCha 合成的——**Wan2.2-Animate 是 SCAIL-2 的教师模型之一**，
SCAIL-2 通过"反向驱动（reverse driving）"训练在替换模式下反超了教师。

选 2.1 而非 2.2 MoE 是刻意的：2.2 是双专家 27B（每步激活 14B），权重体积翻倍，
而 SCAIL-2 要做的 in-context 条件注入（28 通道掩码 + RoPE 改造）在单一 DiT 上
更干净；加上 Wan2.1 的 LoRA 生态成熟。**注意 2.2 的 LoRA 与 2.1 完全不兼容。**

## 2. 主引擎选型：口播场景为什么是 Wan2.2-Animate

两个模型的架构差异决定了各自的擅长面：

| | Wan2.2-Animate | SCAIL-2 |
| :-- | :-- | :-- |
| 底座 | Wan2.2 MoE（27B，激活 14B） | Wan2.1 DiT（14B） |
| 驱动方式 | 骨架（`pose_video`）控肢体 + **人脸裁剪（`face_video`，512×512）经注意力注入表情** | 端到端 in-context，无中间表示 |
| 分块窗口 / 锚定 | 77 帧 / `continue_motion` 5 帧 | 81 帧 / `previous_frames` 5 帧 |
| 强项 | **面部微表情（眨眼、眼神、口型）细腻**，1:1 姿态精确，环境融合有 relight LoRA | 复杂 3D 旋转翻滚、肢体交叠、多人交互、非人角色；低分辨率稳定 |
| 弱项 | 骨架跟踪在复杂旋转/遮挡时会错乱；低分辨率脸部退化 | 微表情细腻度略逊 |

**口播视频的特征是"身体几乎不动、面部微表情与口型占绝对主导"**——正好命中
Wan2.2-Animate 那条专门的面部条件通路，也完全避开了它的弱项（口播没有复杂
3D 动作）。因此主引擎切换为 Wan2.2-Animate，SCAIL-2 保留为 `scail2` 引擎备选。

两者在 ComfyUI 里的锚定接口几乎同构（`continue_motion` ↔ `previous_frames`，
默认都是 5 帧），所以切换只是新增一个引擎类，`LongVideoProcessor` 完全不动。

### 全原生节点的预处理链（本项目的一个关键工程决定）

官方 ComfyUI Wan2.2-Animate 工作流依赖两个第三方节点包，其中 KJNodes 的
`Points Editor` 是**交互式**节点（要人工点击人物位置），API 自动化根本用不了。
本项目改用 ComfyUI 原生节点复现同样的输入：

```
RTDETR_detect(class_name=person)              → 人体框
  → SDPoseKeypointExtractor                   → 全身关键点（含 68 点人脸）
      → SDPoseDrawKeypoints                   → pose_video（骨架，肢体控制）
      → SDPoseFaceBBoxes + CropByBBoxes       → face_video（512×512，表情/口型）
SAM3_VideoTrack + SAM3_TrackToMask            → character_mask（替代 Points Editor）
```

好处：零 `custom_nodes` 依赖，全链路可脚本化，不受第三方节点版本漂移影响。

## 3. wan2.7 能不能用？——能，但要看清它是什么

这一节回应"wan2.7 更像通用模型，角色替换是其中一种能力"。**这个判断是对的**，
但对角色替换任务有两个必须知道的前提。

### 3.1 wan2.7 没有 animate 能力线

百炼上的模型矩阵（逐一核对过）：

| 能力 | 最新模型 | 单次时长 | 锚定接口 |
| :-- | :-- | :-- | :-- |
| 文生视频 / 图生视频 | `wan2.7-t2v` / `wan2.7-i2v` | 2–15s | i2v 有"视频续写" |
| 参考生视频（最多 5 主体） | `wan2.7-r2v` | 2–10s | 无 |
| **通用视频编辑** | `wan2.7-videoedit` | **2–10s** | **最多 4 张参考图** |
| **视频换人** | **`wan2.2-animate-mix`** | 2–30s | **无** |
| 动作迁移 | `wan2.2-animate-move` | 2–30s | 无 |

2.5 → 2.6 → 2.7 迭代的是"生成 / 叙事 / 声画同步 / 编辑"这条线；**animate
（换人、动作迁移）自 2.2 之后没有出过新版本**。所以"用 API 就能用 2.7 做换人"
这个路径不存在——但 `wan2.7-videoedit` 的**指令 + 参考图编辑**确实能做替换
（官方示例里就有"将画面唱歌的女生替换为一只拟人猫咪在唱歌"），这是它作为
通用编辑模型的一种用法。

### 3.2 wan2.7 的长视频方案：参考级伪锚定（本项目已实现）

`wan2.7-videoedit` 没有 `continue_motion` 这类 latent 锚点输入，但它接受
**最多 4 张参考图**——这留出一条弱锚定通路：

```
第 1 块：参考图 = [源角色照片]
第 i 块：参考图 = [源角色照片, 第 i-1 块输出的末帧]
```

模型因此能"看到"上一块里角色最终长成什么样，身份漂移显著小于完全独立生成。

**必须清楚它的本质局限**：新块开头是模型重新生成的像素，与上一块结尾并非同一
批像素。所以 processor 在检测到 `AnchorMode.REFERENCE` 时会**强制 overlap=0**
——此时做 crossfade 只会把两批不同像素叠成鬼影。块边界仍可能有轻微跳变。

这条路适合验证 wan2.7 的画质上限（它是 1080P/30fps、支持声画同步的新模型），
不能替代自托管的 latent 级锚定。启用方式：

```bash
SCAILSWAP_ENGINE=dashscope
DASHSCOPE_MODEL=wan2.7-videoedit
```

### 3.3 三级锚定模式（架构上的抽象）

| 模式 | 机制 | 一致性 | 分块策略 | 代表引擎 |
| :-- | :-- | :-- | :-- | :-- |
| `LATENT` | 上一块输出末 5 帧 VAE 编码后冻结为新块 latent 头部（`noise_mask=0`） | 最强，身份/光影/动作速度由模型保证 | 重叠 5 帧 + crossfade 融合 | `wan_animate`、`scail2` |
| `REFERENCE` | 上一块末帧作为附加参考图 | 中等，身份稳但边界有跳变 | **不重叠**（overlap=0） | `dashscope` + `wan2.7-videoedit` |
| `NONE` | 无 | 无跨块保障 | 只允许单块，多块直接拒绝 | `wan2.2-animate-mix`、`fal` |

`NONE` 模式下 processor 会**拒绝**多块生成而不是硬拼——独立分段拼接就是被
明确排除的"伪造拼接"，身份必然漂移。

## 4. 帧率与时长策略

输出帧率不再强制跟随源视频，拆成两个正交参数：

- `target_fps` —— **生成帧率**。整段驱动视频先重采样到它（ffmpeg `fps` 滤镜，
  **时长不变**），之后所有分块/锚定/拼接都基于它。
- `output_fps` —— **成片帧率**。默认等于 `target_fps`；更高时可选
  `interpolate_output` 用 `minterpolate` 做运动补偿插帧。

**为什么这个改动有实际价值**：Wan 系模型原生就在 16fps 上训练。把 30fps 的口播
素材降到 16fps 生成，帧数直接减半（算力/API 费用同比下降），且更贴合训练分布；
成片再升回 30fps。5 分钟素材从 9000 帧降到 4800 帧，块数从 ~125 降到 ~67。

**音画对齐的关键不变量**：帧率改写始终保持**时长不变**，音轨全程取自源文件、
从未被切割或变速，因此合回来天然对齐——不需要任何音频重定时。

时长无上限：逐块串行 + 断点续传（`state.json`）+ 中间文件即用即删
（`cleanup_intermediate`），5 分钟以上素材同样可跑。托管 API 强制输出的
15/25fps 会在拼接前自动重采样回 `target_fps`。

## 5. 已知取舍与后续可选优化

1. **参考级锚定的边界跳变**：`wan2.7-videoedit` 路线固有限制，除非官方放开
   latent 锚点输入。可尝试把更多历史锚帧塞进 4 张参考图配额来缓解。
2. **多人替换**：`max_objects` / `object_indices` 已暴露，但多参考图
   （背面视图、服装特写）尚未封装。
3. **超长视频（>10 分钟）的漂移**：每块都会重新注入源角色照片作为参考，身份有
   持续重锚；但颜色匹配是链式的（对齐上一块），极长序列理论上仍可能缓慢偏移。
   如遇到，可改为周期性对齐到第一块的末帧。
4. **fal / WaveSpeed 若未来放开锚点输入**：只需把对应引擎的 `anchor_mode`
   改成 `LATENT` 并填上字段，即可获得与自托管一致的长视频能力。
